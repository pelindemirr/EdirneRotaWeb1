// Bu dosya artık kullanılmıyor.
// Login ve Register için ayrı sayfalar oluşturuldu: /auth/login ve /auth/register
// Backup dosyası: LoginModal.backup.tsx

interface LoginModalProps {
  isOpen: boolean;
  onClose: () => void;
}

const LoginModal = ({ isOpen, onClose }: LoginModalProps) => {
  return null;
};

export default LoginModal;
